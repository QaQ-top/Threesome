import Vue from 'vue';
/**
 * @template button 按钮
 */
import { Button } from 'vant';
Vue.use(Button);

/**
 * @template Tab 标签页
 * @template Tabs 标签页
 */
import { Tab, Tabs } from 'vant';
Vue.use(Tab);
Vue.use(Tabs);